const startGameBtn = getElementById('start-game');

startGameBtn.onClick (function () {
    return gameStart();
    }
)

function gameStart () {
    //onclick event of start button
    //hides first div display none
    //displays game div display block
    //get next question
};

function getNextQuestion () {
    //shows the question 
    //hides previous question
};

function displayQuestionAnswer () {
    //show question from data.js
    //show answers in buttons - loop?
};

function chooseAnswer () {
    //Allows to choose answer
    //checks answers correct/incorrect
}

function calculateResults () {
    //displays end game div
    //shows results
    //user can return to try again
}